# Copie este arquivo para ".env.local" (dentro de frontend/) e preencha com os valores reais.
#
# API_URL é server-only (sem prefixo NEXT_PUBLIC_): o browser nunca fala diretamente com o
# FastAPI, só o BFF (Route Handlers do Next). Ver plan/Plano de Implementacao do Front-end -
# Dashboard Next.js.md.

API_URL=http://localhost:8000
